apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: evoeventmem
  labels:
    app.kubernetes.io/name: evoeventmem
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: evoeventmem
    app.kubernetes.io/component: api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
