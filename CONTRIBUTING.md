# Contributing

1. Select one task ID from `TASKS.md`.
2. Create a branch `task/<id>-<slug>`.
3. Do not mix optional work into a mainline task.
4. Run the task-specific verification plus `make check` when available.
5. Include generated benchmark artifacts for any metric claim, but never commit raw datasets or secrets.
