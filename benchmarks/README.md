# Benchmarks

M02–M15 在此实现规范化、基线、运行器、评测与分析。当前只提供数据 manifest；不要在不同 benchmark 目录复制一套不兼容的模型和 artifact 接口。

预期最终结构：

```text
benchmarks/
├── common/
├── longmemeval/
├── locomo/
├── analysis/
└── manifests/
```
